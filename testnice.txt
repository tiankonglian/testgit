1.统一设备状态接口，各个界面调统一的接口获得设备状态防止状态不同步。
2.减少各个界面维护的connect service。
3.存储尽量使用数据库，写文件容易乱。

1.不使用的代码直接删除，共用的方法尽量放到Util中
2.没有必要缓存的数据就不要保存到本地，直接提示网络异常
3.协议固定的接口都放到NetService中

ios相对历史遗留问题少一点，我们再看看有什么要优化的，目前看到的是代码要增加注释

1.Android的onCreate（或者Fragment的onCreateView）中不要放太多代码，通过initViews、initData、initListener等进行封装。
2.代码中尽量不要出现硬编码，字符串写在资源文件里面，常量值写在公共类里面。

1.文档内容需要规范，统一，简单，清晰，便于阅读